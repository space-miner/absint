(executable
 (public_name absint)
 (name main)
 (modules astprint absint extinct binterval util lexer main parser syntax)
 (libraries base stdio stdlib zarith)
 (preprocess
  (pps ppx_jane ppx_sexp_conv)))

(ocamllex lexer)

(menhir
 (modules parser))
